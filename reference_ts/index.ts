import init from './command/version-control/init';

/**
 * CS01 Version Control System - Core API.
 * @module CS01
 * @example
 * import { init } from 'cs01';
 * init({ bare: true });
 */
export { init };

export default {
  init
};